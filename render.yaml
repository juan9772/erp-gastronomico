databases:
  - name: erp-db
    plan: free
    databaseName: erp_backend
    user: erp_user
    region: ohio

services:
  - type: web
    name: erp-web
    env: docker
    plan: free
    region: ohio
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: erp-db
          property: connectionString
      - key: APP_KEY
        generateValue: true
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: LOG_CHANNEL
        value: stderr
      - key: RENDER
        value: "true"
